// these are JS constants representing our SASS transitions

const TRANSITIONS = {
    base: '0.1s ease-in-out',
    toggle: '0.3s cubic-bezier(0.17,0.67,0.53,1)',
    animateClip: '0.25s ease-in-out',
};

export default TRANSITIONS;
